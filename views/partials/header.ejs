<!DOCTYPE html>
<html>
    <head>
        <title>Notes on Books</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Merriweather&display=swap" rel="stylesheet">
        <link href="/styles/main.css" rel="stylesheet">
    </head>
    <body>
        <section id="header">
            <div class="container border-bottom my-4">
                <h1 class="text-center">Notes on Books</h1>
                <h3 class="text-center text-body-primary">Make notes on books you have read.</h3>
                <div class="row-md-3 text-end mb-1">
                    <form>
                        <input id="login-button" type="<% if ( locals.authenticating ) {%>hidden<% } else { %>submit<% } %>" class="btn btn-outline-secondary d-inline-flex align-items-center"
                            value="<% if ( locals.hideLogin ) {%>Logout<% } else { %>Login<% } %>"
                            formaction="<% if ( locals.hideLogin ) {%>/logout<% } else { %>/login<% } %>"
                            formmethod="<% if ( locals.hideLogin ) {%>post<% } else { %>get<% } %>">
                        <input id="signup-button" type="<% if ( locals.hideSignup ) {%>hidden<% } else { %>submit<% } %>" class="btn btn-primary d-inline-flex align-items-center" value="Sign-up" formaction="/signup" formmethod="get" >
                    </form>
                </div>
            </div>
        </section>
