<%- include("partials/header.ejs") %>

    <div class="container">
        <% if ( locals.errmsg ) { %>
            <h2 class="text-center"><%= errmsg %></h2>
        <% } else { %>
            <h2 class="text-center">Due to technical difficulties, the service is unavailable currently.</h2>
            <h2 class="text-center">Sorry for the incovenience.</h2>
        <% } %>
    </div>
    <div class="row">
        <form action="/%>" method="get">
            <div class="d-flex gap-2 justify-content-center py-1">
                <input type="submit" class="btn btn-outline-secondary d-inline-flex align-items-center text-wrap" type="button" id="home-button" value="Return to main page">
            </div>
        </form>
    </div>

<%- include("partials/footer.ejs") %>